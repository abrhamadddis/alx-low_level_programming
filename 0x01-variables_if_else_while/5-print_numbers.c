#include <stdio.h>
/**
 *main - starting point
 * Return: 0
 *
 * Discripition: a progrma that print a single digit numbers of base 10
 */
int main(void)
{
int bu;
	for (bu = 0; bu < 10; bu++)
	{
		printf("%d", bu);
		printf("\n");
	}
return (0);
}
