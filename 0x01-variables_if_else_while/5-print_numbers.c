#include <stdio.h>
/**
 *main - starting point
 * Return: 0
 *
 * Discripition: a progrma that print a single digit numbers of base 10
 */
int main(void)
{
int i;
	for (i = 0; i < 10; i++)
	{
		putchar(i);
		putchar('\n');
	}
return (0);
}

