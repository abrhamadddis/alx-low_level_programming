#include <stdio.h>
/**
 *main - Starting point
 *
 *Return: zero
 *
 *Descripitions: Prints single digit number
 */

int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
	printf("%d", num);
	}
	return (0);
}
