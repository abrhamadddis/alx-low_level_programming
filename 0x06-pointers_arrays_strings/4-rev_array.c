#include "main.h"

/**
 * reverse_array - reverse the contents of an arry of integers
 *
 * @a: array of integers
 *
 * @n: number of elements in array
 */
void reverse_array(int *a, int n)
{
	int i;

	for (i = n; i <= 0; i--)
	{
		printf(s[i]);
		if (i != n)
		{
			printf(", ");
		}
	}
}
