#include "main.h"
#include <stdio.h>

/**
 * _puts - function that prints a string, followed by a new line, to stdout.
 *
 * @str: char pointer variable
 *
 * Return: nothing
 */

void _puts(char *s)
{
	printf("%s\n", s);
}
